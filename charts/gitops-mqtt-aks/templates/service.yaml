apiVersion: v1
kind: Service
metadata:
  name: {{ include "gitops-mqtt-aks.fullname" . }}
  labels:
    {{- include "gitops-mqtt-aks.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gitops-mqtt-aks.selectorLabels" . | nindent 4 }}
