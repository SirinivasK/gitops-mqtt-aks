# Add this to your existing values.yaml
replicaCount: 1

mqttBroker:
  image: eclipse-mosquitto:latest
  name: mqtt-broker
  ports:
    - name: mqtt
      containerPort: 1883

mqttListener:
  image: eclipse-mosquitto:latest
  name: mqtt-listener